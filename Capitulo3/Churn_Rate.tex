%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%           Capítulo 3: MARCO TEÓRICO - REVISIÓN DE LITERATURA
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Metodología de análisis y preparación de los datos}

\section{Descripción del conjunto de datos}

Una parte crucial en toda implementación de Machine Learning es la calidad de los datos, y es algo que cuando se hacen implementaciones sobre información real cobra mucha relevancia, ya que en términos generales es deber del científico de datos a cargo, asegurarse que dicha información sea de calidad.

Ya sea asegurarse del cumplimiento de los supuestos que cada modelo de machine learning demanda ó bien, asegurarse que la calidad de la información sea la adecuada para poder obtener las mejores estimaciones y evitar problemas de Under-fitting o Over-fitting.


En la mayoría de las implementaciones de Machine Learning necesitamos recolectar la información desde alguna fuente de origen, ya sea una base de datos, data lake o cual sea la fuente de la que provengan nuestros datos, por lo cual sera de vital importancia tener total conocimiento acerca del sistema de origen de la información. En consecuencia parte del pipeline que se va a preparar comprende la extracción de la información y en algunos casos preparar un primer procesamiento que nos asegure trabajar con el nivel de especificación que deseamos.

Para el caso particular presentado en este trabajo, es necesario asegurarnos que el nivel máximo de detalle en nuestros datos sean clientes, osea que cada campo y registro debe hacer referencia a información de los clientes, como su historial de compras, año de registro, etc. Y por lo tanto un primer paso sería validar que estemos trabajando con registros únicos.

En consecuencia es importante conocer todos los detalles posibles de los datos con los que vamos a trabajar, tanto en lo concerniente al negocio o a las propiedades inherentes de los datos, esto sera mejor abordado en el siguiente apartado.

\section{Preprocesamiento}

\subsection{Limpieza y tratamiento de valores faltantes y outliers}

\subsection{Transformaciones y codificación}

\subsection{Selección y reducción de características (PCA)}

\section{Construcción de la variable de abandono (churn)}

En este capítulo abordaremos el concepto de abandono de clientes, al ser este uno de los fenómenos que deseamos analizar y predecir, es de suma importancia comprender el concepto de abandono, esto para tener completo entendimiento sobre el comportamiento de los clientes y el fuerte impacto que puede tener el conocer el abandono dentro de una organización. En adelante nos referiremos al abandono como \textit{churn} y a la tasa de abandono como \textit{churn rate}.

La forma que usaremos para calcular la tasa de abandono en este trabajo sera la dada por \ref{EquationChurn}, como podemos apreciar esta expresión considera cierto enfoque temporal, más adelante abordaremos el porque.

\begin{equation}
	\textrm{Churn Rate}=\frac{\textrm{Número de clientes en el ultimo periodo}}{\textrm{Total de clientes}} -1 
	\label{EquationChurn}
\end{equation}

De hecho si tomamos la razon entre el numero de clientes en el ultimo periodo o en el actual y el total de clientes tendremos la tasa de retención de clientes. Entendiendo que la tasa de retención es el complemento de la tasa de abandono.
 




\newpage
\section{Implementación de modelos de ML para predecir el abandono}

Una vez entendido el concepto de retención y abandono de clientes y las tasas y el análisis de cohorte, comenzaremos por mostrar la implementación de algoritmos de Machine Learning que nos permitan realizar predicciones del abandono. Recordemos que definimos la columna churn (abandono) como una variable de tipo binaria, donde true indica que un cliente abandono y false el caso contrario, en este caso nuestra variable objetivo sera la columna churn. 

En esta sección nos apoyaremos de las herramientas vistas en la sección 2 y más en especifico llevaremos acabo la implementación de modelos de clasificación binaria para poder predecir las etiquetas de la variable churn, por lo cual no profundizaremos mucho en la teoría detrás, sino nos enfocaremos unicamente en los resultados obtenidos y la interpretación de los mismos.

Para este trabajo compararemos los resultados de distintos modelos de clasificación binaria, tomando en cuenta las metricas de evaluación como precisión, exactitud, etc, haciendo uso tanto de herramientas visuales y resúmenes estadísticos veremos las desventajas y ventajas de cada modelo, esto eventualmente nos conducirá a determinar el modelo que mejor realice la clasificación.


Antes de empezar con el análisis comparativo, es conveniente aclarar que los modelos que en adelante analizaremos ya fueron optimizados y entrenados en el capitulo anterior, por lo cual en esta sección tomaremos como punto de partida lo visto en el capitulo anterior. 
% Duda sobre esta sección.

\subsection{Evaluación del modelo.}

Previo a la profundización en cada modelo y sus resultados veremos como evaluar el rendimiento de cada modelo, en esta sección utilizaremos como ejemplo el modelo multi-layer perceptron classifier, por lo cual lo visto de aquí en adelante es fácilmente reproducible a cualquier otro modelo. Partiremos de las predicciones con el conjunto de datos de validación, así nos apoyaremos de la matriz de confusión y curva roc para evaluar nuestros modelos de forma visual y numérica.

\begin{figure}[h]
	%\vspace{-55mm}
	%\hspace*{-5cm}
	\centering
	\includegraphics[scale=.4]{Gráficas e Imagenes/Confusion_matrix_train.png}
	\caption{Matriz de confusión para el modelo de multi-layer perceptron classifier.}
	\label{fig:MatrizConfusion}
\end{figure}

En la gráfica \ref{fig:MatrizConfusion} podemos apreciar el numero de aciertos (verdaderos positivos y negativos) y el numero de errores (falsos positivos y negativos) cometidos por el modelo. La representación gráfica anteriormente mencionada es la matriz de confusión asociada al modelo de multi-layer perceptron classifier, de hecho esta gráfica nos permite encontrar la mayoría de las metricas para evaluar nuestro modelo. Por ejemplo, la ecuación \ref{EquationAcurracy}\footnote{Las variables TP, TN,FP y FN son el numero de casos true positives, true negatives, false positives y false negatives respectivamente.} nos proporciona la \textit{accuracy} o exactitud, esta métrica nos indica el porcentaje de acierto que tuvo el modelo. Python nos facilita el calculo de esta métrica, el código \ref{pymetrics} muestra un ejemplo de como calcular la accuracy haciendo uso del sklearn.

\textbf{Aquí pienso colocar el análisis de la matriz de este modelo, decidí no añadirlo por que puede cambiar.}

\begin{equation}
	\textrm{accuracy}=\frac{TP+TN}{(TP+TN+FP+FN)} 
	\label{EquationAcurracy}
\end{equation}

Sin embargo \citet{2023Peterson} menciona que la accuracy usualmente no es una buena métrica para evaluar modelos cuando tenemos clases desbalanceadas en nuestra variable objetivo, lo cual es nuestro caso, ya que como mencionamos anteriormente la variable \textit{churn} tiene más registros marcados como \textit{false}, aun que en el capitulo anterior explicamos la forma en que lidiamos con el desbalance de las clases, explicaremos por que no siempre es conveniente optar por la exactitud, esto porque puede encubrir un incorrecto funcionamiento del modelo, por ejemplo, la matriz \ref{fig:MatrizConfusion} podemos notar que el total de las observaciones que inicialmente fueron clasificadas como false son mayores a las clasificados como true, el problema es que aunque el porcentaje de aciertos en la clase con dominante es medianamente aceptable, siento mayor a $90\%$, la clase minoritaria muestra un menor rendimiento, \textbf{insertar porcentaje} esta cifra indica el mal rendimiento para los registros clasificados como true.

Y es que aun que la clase true sea minoritaria, nos interesa tener la mayor cantidad de predicciones correctas para que en un futuro poder predecir correctamente los clientes que están abandonaran la compañía. Por lo tanto recomendamos optar por otras metricas para tener una visión mas completa del rendimiento de nuestro modelo. 

\begin{lstlisting}[language=Python, caption=código para calculo de metricas, label=pymetrics]
	from sklearn.metrics import accuracy_score,confusion_matrix, classification_report, precision_score, recall_score, f1_score
	print("Accuracy: {}".format(accuracy_score(y_true=valid['y'],y_pred=valid['y^_MLP'])))
	print("Precision: {}".format(precision_score(y_true=valid['y'],y_pred=valid['y^_MLP'])))
	print("Recall: {}".format(recall_score(y_true=valid['y'],y_pred=valid['y^_MLP'])))
	print("F1: {}".format(f1_score(y_true=valid['y'],y_pred=valid['y^_MLP'])))
\end{lstlisting}

Algunas de las metricas a las que podemos recurrir son precision (precisión), recall (sensibilidad o recuperación) y $F_\beta$. Aun que existe muchas otras metricas, nosotros nos enfocaremos solo en estas. La precisión (ecuación \ref{Equationprecision}) es el número de aciertos, predicciones correctas, entre el total de casos positivos captados por el modelo, osea del total de casos clasificados como true, cuales son realmente true, mientras que la sensibilidad (ecuación \ref{EquationRecall}) es el numero de predicciones correctas, entre el total de casos positivos, es decir es la proporción de casos clasificados como true.

\begin{equation}
	\textrm{precision}=\frac{TP}{(TP+FP)} 
	\label{Equationprecision}
\end{equation}

\begin{equation}
	\textrm{recall}=\frac{TP}{(TP+FN)} 
	\label{EquationRecall}
\end{equation}

Hay distintas ventajas de usar una u otra métrica para evaluar nuestro modelo, si nosotros decidimos inclinarnos por la recall, estaríamos dando mayor importancia a reducir la cantidad de errores cometidos por el modelo, esto nos indica que esta métrica no nos aporta información acerca de los falsos positivos, y lo mismo pasa con la presicion, ya que al querer reducir el numero de falsos positivos estaríamos ignorando el rendimiento del modelo frente los falsos negativos, por lo cual escoger una u otra es decisión del científico de datos a cargo, en el código \ref{pymetrics} nos muestra como calcular estas dos metricas con ayuda de Python.

\begin{equation}
	F_\beta=(1+\beta^2)\cdot \frac{\textrm{precision}\cdot \textrm{recall}}{\beta^2\cdot(\textrm{precision}+\textrm{recall})} 
	\label{EquationFB}
\end{equation}

La métrica que nos permite encontrar un equilibrio entre el numero de FP y FN es el F-score, esta métrica combina tanto la precision y recall en un solo valor, la ecuación \ref{EquationFB} nos permite calcular dicho valor. El parámetro de $\beta$ nos da la posibilidad de otorgarle un nivel de importancia a la precision o recall. De hecho si el valor de $\beta$ es igual a cero el F-score le da mayor importancia a la precision, mientras que cuando $\beta$ es igual a 0.5 el $F-score$ le da mayor peso a la recall, por ultimo si $\beta$ es igual a 1, obtenemos el $F_1$-score (ecuación \ref{EquationF1}) le damos igual importancia a ambas metricas, de esta forma podemos evaluar tanto el número de falsos positivos y negativos.


\begin{equation}
	F_1=2\cdot \frac{\textrm{precision}\cdot \textrm{recall}}{(\textrm{precision}+\textrm{recall})} 
	\label{EquationF1}
\end{equation}


De la comparación entre precision y recall, surge otra forma de evaluar modelos de clasificación binaria, esta métrica es la curva roc (Receiver Operating Characteristic), \citet[p. 91]{2017Géron} nos dice que \textit{la curva ROC representa gráficamente la tasa de verdaderos positivos frente a la tasa de falsos positivos. La FPR es la proporción de casos negativos que se clasifican incorrectamente como positivos.} 

\begin{figure}[h]
	%\vspace{-55mm}
	%\hspace*{-5cm}
	\centering
	\includegraphics[scale=.6]{Gráficas e Imagenes/curva_roc_valid.png}
	\caption{Curva ROC.}
	\label{fig:CurvaRoc}
\end{figure}

En la gráfica \ref{fig:CurvaRoc} podemos notar que el eje de ordenas esta etiquetado como True Positive Rate (TPR), mientras que el eje de abscisas esta etiquetado como False Positive Rate (FPR), donde las TPR puede ser también interpretado como la recall mientras que FPR suele interpretarse como el complemento de la especificidad (1- especificidad), \citep{2017Géron} menciona que un buen clasificador se mantiene lo más posible alejado de la linea vertical punteada, dicha linea diagonal sirve más como una referencia y es también es llamada línea de no-discriminación.

Además de la curva roc, el área bajo la curva roc (comúnmente llamada AUC) es otra muy buena opción para medir el rendimiento de modelos de clasificación binaria, ya que un valor cercano a 1 de la AUC nos indicara que el contamos con un buen clasificador, este buen rendimiento se considera respecto a la especificidad y recall, es decir, estas dos metricas nos indica que nuestro modelo en cuestión dará una mejor calificación a la probabilidad de un registro positivo seleccionado al azar que a un registro negativo seleccionado de forma aleatoria.

De hecho este ultimo punto deja en evidencia que existe cierto inconveniente al evaluar nuestros modelos con la auc solamente, ya que la curva roc puede encubrir desbalances entre clases de la variable objetivo, \cite{Chugh2023} plantea que la curva roc tiende a pasar por alto el desequilibrio entre clases, ya que la tasa de FP es una métrica que solo contempla a la clase negativa, lo que significa que se espera que el cambio en FP sea proporcional al cambio en FP+TN (todos los casos negativos), esto puede derivar en tener poca sensibilidad a cambios en la distribución de las clases de la variable objetivo.

Partiendo de este ultimo punto y recordando que en nuestro caso la información que estamos analizando tiene cierto desequilibrio entre clases de la variable a predecir, sin embargo no todo esta perdido, ya que existe otra métrica que nos proporciona mayor información que la auc y que no pasa por alto el desequilibrio en entre clases, esta métrica es la curva precision-recall, en palabras de \citet{Chugh2023} \textit{una curva ROC es similar a la curva PR (Precision Recall), pero traza la tasa de verdaderos positivos (TPR) frente a la tasa de falsos positivos (FPR) para diferentes umbrales... La PR es comparativamente más informativa. Esto se debe a que la curva P-R proporciona información más significativa sobre la clase de interés en comparación con la curva ROC.} 


\begin{figure}[h]
	%\vspace{-55mm}
	%\hspace*{-5cm}
	\centering
	\includegraphics[scale=.6]{Gráficas e Imagenes/precision_recall_curve_with_area.png}
	\caption{Curva PR.}
	\label{fig:CurvaPR}
\end{figure}

Por lo tanto la curva PR nos ayudara a evaluar de mejor manera nuestros datos dando mayor importancia a los registros positivos pero sin dejar completamente de lado los negativos, encontrando así un equilibrio no arbitrario entre uno y otro. Esto porque uno de los grandes inconvenientes de elegir la auc sobre la PR es que en nuestro caso al tener mayor proporción de registros clasificados negativamente (osea como no abandono) hace que la tasa de FP sea demasiado pequeña, en comparación de la PR que al considerar la a la precision y por ende el rendimiento de nuestro modelo en los registros positivos.

\subsection{Comparación del rendimiento entre modelos.}

Analizaremos el rendimiento de nuestros modelos en el conjunto de entrenamiento y de validación haciendo uso de las metricas antes mencionadas. Por ejemplo, en la matriz de confusión del modelo de regresión logística para el conjunto de entrenamiento (\ref{fig:MatrizLogis} izquierda) podemos notar signos de un mal desempeño a la hora de predecir correctamente los registros positivos (abandonos) con respecto de los casos negativos (no abandonos), esto se hace más evidente en el caso de los falsos positivos, cuya presencia es mayor que los verdaderos positivos, lo cual se traduce inmediatamente en una precisión baja. Sin embargo en el conjunto de validación (derecha \ref{fig:MatrizLogis}) podemos notar que este comportamiento persiste.



\begin{figure}[h]

	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_logistica_train.png} 
		%\caption{}
		\label{fig:subim1}
	\end{subfigure}
	\hfill
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_logistica_valid.png}
		%\caption{}
		\label{fig:subim2}
	\end{subfigure}
	
	\caption{Matriz de confusión para el modelo de regresión logística del conjunto de entrenamiento y validación}
	\label{fig:MatrizLogis}
\end{figure}

De hecho nos podemos auxiliar de la gráfica de presicion vs recall (\ref{fig:CurvaPRLogist}) trazada para el modelo de regresión lineal, en este caso claramente podemos notar que nuestro modelo no esta teniendo un buen desempeño, esto se puede hacer más evidente si analizamos la accuracy, cuyo valor es demasiado bajo. En la gráfica \ref{fig:MetricsLogist} podemos ver el desempeño de cada una de las metricas antes mencionadas en nuestro modelo, vemos que tanto la accuracy y el F1 score tienen un mal desempeño, esta gráfica nos confirma el mal desempeño de este modelo tanto en el conjunto de entrenamiento y validación.


%\begin{figure}[h]
%	\centering
%	\includegraphics[width=0.5\linewidth, height=5cm]{Gráficas e Imagenes/metricas_valid_logistic.png}
%	\caption{Metricas de evaluación del modelo de regresión logística para %el conjunto de entrenamiento y validación}
%	\label{fig:MetricasLogist}
%\end{figure}


\begin{figure}[h]
	%\vspace{-55mm}
	%\hspace*{-5cm}
	\centering
	\includegraphics[scale=.3]{Gráficas e Imagenes/precision_recall_curve_with_area_logistica.png}
	\caption{Curva PR de Regresión Logística}
	\label{fig:CurvaPRLogist}
\end{figure}

Por ultimo inspeccionando la curva roc del modelo de regresión logística en la gráfica \ref{fig:RocCurves}, podemos notar los vicios antes mencionados en nuestro modelo, dado que el ``ideal" de un modelo es tener una area (auc) lo más cercana a uno, en la curva roc del conjunto de entrenamiento y validación del modelo de regresión logística podemos notar que la $AUC_{train}=0.86$ y $AUC_{valid}=0.85$, esto aunado a que la curva roc es bastante cercana a la linea de no-discriminación, por lo cual podemos considerar a este modelo como un mal clasificador, dado que no cumple con el rendimiento esperado, por las razones antes mencionadas, lo cual eventualmente causaría conflictos, pues hay mayor posibilidad de clasificar de forma equivocada a un cliente como abandonador.

\begin{figure}[h]
	\centering
	\includegraphics[scale=.3]{Gráficas e Imagenes/metricas_logistic.png}
	\caption{Metricas de evaluación del modelo de regresión logística para el conjunto de entrenamiento y validación}
	\label{fig:MetricsLogist}
\end{figure}

Por otra parte, el modelo KNN nos ofrece características más interesante, de inicio la matriz de confusión (\ref{fig:MatrizKNN}) tanto del conjunto de entrenamiento y validación a diferencia del modelo anterior podemos ver un menor numero de casos falsos positivos y por ende un mejor rendimiento a la hora de clasificar los casos verdaderamente negativos y positivos, si analizamos damos una rápida inspección a las metricas de evaluación del modelo podemos notar un mejor rendimiento en la presicion y por ende tenemos un mejor F1 score.

\begin{figure}[H]
	
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_knn_train.png} 
	\end{subfigure}
	\hfill
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_knn_valid.png}
	\end{subfigure}
	
	\caption{Matriz de confusión para el modelo KNN del conjunto de entrenamiento y validación}
	\label{fig:MatrizKNN}
\end{figure}

Esto ultimo lo podemos confirmar con la gráfica \ref{fig:Metricsknn} en donde podemos ver una mejora significativa en el rendimiento del modelo, teniendo una mejor accuracy e F1 score, sin embargo el modelo presenta un inconveniente que podemos apreciar. El rendimiento del modelo decrece en el conjunto de validación, esto con respecto del conjunto de entrenamiento, esto nos indica que este modelo en especifico no generaliza del todo bien las predicciones, de hecho esto se hace evidente en las variaciones tan marcadas en las metricas que muestra el modelo, por ejemplo podemos ver que tan solo la accuracy del conjunto de pruebas y validación varia por 0.05, y que el F1 score varia 0.11, lo cual es una gran diferencia en el rendimiento de nuestro modelo, esto nos da indicios de que este modelo podría volverse obsoleto en el corto o mediano plazo, lo cual podría repercutir en futuros errores de clasificación.


\begin{figure}[h]
	\centering
	\includegraphics[scale=.3]{Gráficas e Imagenes/metricas_knn.png}
	\caption{Metricas de evaluación del modelo de KNN para el conjunto de entrenamiento y validación}
	\label{fig:Metricsknn}
\end{figure}

De igual forma la curva roc del modelo KNN que podemos apreciar en las gráficas \ref{fig:RocCurves} vemos este decaimiento en el rendimiento en el conjunto de validación, notemos como la $AUC_{train}=0.96$ baja $AUC_{valid}=0.90$ lo cual confirma que este modelo no es confiable y por tanto lo descartaremos como modelo final para clasificar clientes.

\begin{figure}[h]
	
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_svm_train.png} 
		%\caption{}
		\label{fig:subim1}
	\end{subfigure}
	\hfill
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_svm_valid.png}
		%\caption{}
		\label{fig:subim2}
	\end{subfigure}
	
	\caption{Matriz de confusión para el modelo de soport vector machine del conjunto de entrenamiento y validación}
	\label{fig:MatrizSVM}
\end{figure}

Ahora vamos a analizar los resultados del modelo soport vector machine (svm), de forma análoga inspeccionaremos brevemente las matrices de confusión del modelo, podemos notar en primera instancia (gráfica \ref{fig:MatrizSVM}) que el comportamiento del clasificador en el conjunto de entrenamiento y validación es similar al comportamiento mostrado por los dos modelos anteriores en dichos conjuntos de datos.

\begin{figure}[H]
	\centering
	\includegraphics[scale=.3]{Gráficas e Imagenes/metricas_svm.png}
	\caption{Metricas de evaluación del modelo de SVM para el conjunto de entrenamiento y validación}
	\label{fig:MetricsSVM}
\end{figure}

De igual manera, al inspeccionar las metricas del modelo podemos darnos cuenta que de hecho el comportamiento de los valores de dichas metricas es muy similar al modelo KNN, en ambos modelos podemos notar diferencias considerables entre  el rendimiento mostrado en el conjunto de entrenamiento y validación, teniendo una variación de $0.07$ en accuracy y $0.15$ en la F1 score.

En cuanto a la curva roc podemos notar de igual manera un comportamiento similar, en la gráfica \ref{fig:RocCurves} podemos notar que en ambos modelos el comportamiento en el conjunto de entrenamiento es similar, aun que el clasificador SVM esta un poco por debajo del modelo KNN, de hecho es gracias a la AUC del modelo que podemos confirmar este hecho, ya que la $AUC_{train}$ del modelo KNN es igual $0.96$ mientras que $AUC_{train}$ de SVM es igual a $0.93$, ambos valores son muy buenos, sin embargo al comparar el rendimiento de dichos clasificadores en el conjunto de validación es donde podemos ver las diferencias entre ambos modelos. En este caso SVM no baja demasiado su rendimiento con respecto al conjunto de validación, podemos ver que la $AUC_{valid}$ es igual a $0.90$, por lo cual la variación en el rendimiento entre conjunto de entrenamiento y validación es de $0.03$. 

Entonces podemos ver que a diferencia del modelo KNN, el clasificador SVM no presenta síntomas de sobre ajuste, a deferencia del modelo anterior, por lo cual SVM se perfila a ser un muy serio candidato a ser nuestro modelo final.


Finalmente analizaremos los resultados del modelo Random Forest, de forma parecida a los anteriores modelos podemos percatarnos que las matrices de confusión de los conjuntos de entrenamiento y validación (fig. \ref{fig:MatrizRF}) parecieran tener un rendimiento muy similar a los modelos anteriores, sin embargo mirando un poco más detenidamente nos daremos cuanta que de hecho su rendimiento mayor al de otros modelos como SVM y KNN, ya que el número de FN y FP es más bajo en comparación de los modelos ya mencionados. 

\begin{figure}[h]
	
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_rf_train.png} 
		%\caption{}
		\label{fig:subim1}
	\end{subfigure}
	\hfill
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Confusion_matrix_rf_valid.png}
		%\caption{}
		\label{fig:subim3}
	\end{subfigure}
	
	\caption{Matriz de confusión para el modelo random forest del conjunto de entrenamiento y validación}
	\label{fig:MatrizRF}
\end{figure}

Este ultimo punto lo podemos reforzar revisando la gráfica \ref{fig:MetricsRF}, en la misma ver la alta efectividad de este modelo en el conjunto de entrenamiento, teniendo una accuracy de $0.94$ y un $F_1$ de 0.9 siendo valores muy aceptables, sin embargo esta misma gráfica nos permite apreciar un gran inconveniente con este modelo, ese problema es el sobre ajuste, ya que en tanto la accuracy y el $F_1$ score tienen un bajo de rendimiento importante en el conjunto de validación, siendo de 0.7 y 0.16 para la accuracy y el $F_1$ respectivamente,  lo cual claramente implica que este modelo no generalizara adecuadamente nuestra predicciones y no lo vuelve un mal candidato como modelo final a elegir.

\begin{figure}[H]
	\centering
	\includegraphics[scale=.3]{Gráficas e Imagenes/metricas_rf.png}
	\caption{Metricas de evaluación del modelo de random forest para el conjunto de entrenamiento y validación}
	\label{fig:MetricsRF}
\end{figure}

Recapitulando, hemos comparado distintos modelos aplicado las metricas anteriores, enfocándonos sobre todo en tener un buen $F_1$-score y Precision, por ejemplo el modelo de\textbf{regresión logística} muestra un desempeño bajo, ya que no logra discriminar correctamente entre los clientes abandonadores y no abandonadores, por otro lado el modelo \textbf{KNN} tiene un mejor rendimiento que el modelo anterior, sufre de sobreajuste, lo cual nos lleva a concluir que no va a generalizar bien, por lo cual tenemos un potencial riesgo de obsolescencia en predicciones futuras, sin embargo en términos generales el modelo que cumple con el mejor desempeño general es el \textbf{SVM} esto debido a que tiene el mejor balance entre el conjunto de entrenamiento y validaciones, tiene mejor capacidad de generalización que el modelo anterior y por ultimo tenemos el modelo con el rendimiento mas alto en los datos de entrenamiento, pero en disminuye bastante su rendimiento en el conjunto de validación, lo cual es un claro ejemplo de sobre ajuste y por lo cual es un modelo que no generalizara bien.

\begin{figure}[h]
	
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Roc_Curve_train.png} 
	\end{subfigure}
	\hfill
	\begin{subfigure}
		\centering
		\includegraphics[width=0.5\linewidth, height=4.5cm]{Gráficas e Imagenes/Roc_Curve_valid.png}
	\end{subfigure}
	
	\caption{Curvas Roc de los modelos de clasificación para el conjunto de entrenamiento y validación respectivamente.}
	\label{fig:RocCurves}
\end{figure}

\subsection{IA Explicable.}


Dicho lo anterior, y con el propósito de interpretar de mejor manera el comportamiento de nuestro modelo final de clasificación, vamos a utilizar técnicas de \textbf{Explanable IA (XAI)}, todo esto apoyándonos de la librería SHAP, como dato curioso esta libería debe su nombre a \texttt{\textbf{SH}apley \textbf{A}dditive ex\textbf{P}lanations} que a su vez debe su nombre a Lloyd Stowell Shapley quien fuera un destacado matemático y economista estadounidense, y entre sus grandes aportes a la teoría de juegos y a la ciencia de datos se encuentran los valores de Shapley, los cuales son la base de los valores Shap. La ideas es simple en ejecución, según la documentación de la librería "SHAP es un enfoque de teoría de juegos para explicar la salida de cualquier modelo de aprendizaje automático". 

El propósito de esta metodología es poder dar una explicación clara de como los modelos muy complejos toman decisiones, en nuestro caso saber como el modelo de Sopport Vector Machine decide si un cliente es abandonador o no. Si bien nos podemos dar una idea acerca del funcionamiento de este modelo, usando SHAP podemos saber como cada característica impacta en las predicciones de nuestro modelo, de tal manera que "las características con valores SHAP positivos tienen un impacto positivo en la predicción, mientras que las que tienen valores negativos tienen un impacto negativo. La magnitud es una medida de cuán fuerte es el efecto." \textbf{cita}

\begin{figure}[H]
	\centering
	\includegraphics[scale=.37]{Gráficas e Imagenes/SHAP_decision_plot.png}
	\caption{Gráfica de resumen global}
	\label{fig:SummaryPlot}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[scale=.37]{Gráficas e Imagenes/SHAP_force_plot.png}
	\caption{Gráfica de explicación local}
	\label{fig:ForcePlot}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[scale=.37]{Gráficas e Imagenes/SHAP_decision_plot.png}
	\caption{Gráfica de decisiones}
	\label{fig:DecisionPlot}
\end{figure}

%DataCamp. (Año). Marketing Analytics: Predicting Customer Churn in Python. Recuperado de DataCamp.
